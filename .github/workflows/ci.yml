name: ci 
on:
  push:
    branches:
      - master 
      - main
permissions:
  contents: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: 3.x
      - uses: actions/cache@v2s
        with:
          key: ${{ github.ref }}
          path: .cache  
      - run: apt install python3-pip python3-apt python3-cffi python3-brotli -y
      - run: apt install ibpango-1.0-0 libharfbuzz0b libpangoft2-1.0-0 libgtk-3-dev gcc -y
      - run: pip3 install mkdocs-material
      - run: pip3 install mkdocs-minify-plugin
      - run: pip3 install mkdocs-redirects
      - run: pip3 install pillow libcairo2 libcairo2-dev cairosvg
      - run: mkdocs gh-deploy --force
